enum Role {
  Admin
  User
}

model User {
  id         String   @id @default(uuid())
  email      String   @unique
  name       String
  surname    String
  password   String
  rating     Int      @default(0)
  role       Role     @default(User)
  created_at DateTime @default(now())
}
